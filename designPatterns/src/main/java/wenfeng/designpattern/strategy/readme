案例： 游客(Traveler)出行,有不同的出行方案(飞机 高铁等等)   线程池的拒绝策略

策略模式把对象本身和运算规则区分开来，整个模式也分为三个部分
    环境类(Context):               用来操作策略的上下文环境，也就是我们游客。
    抽象策略类(Strategy):            策略的抽象，出行方式的抽象
    具体策略类(ConcreteStrategy):    具体的策略实现，每一种出行方式的具体实现。